apiVersion: v1
  kind: Service
  metadata:
    name: api-service
    namespace: geocore
    labels:
      app: geocore-api
  spec:
    type: ClusterIP
    selector:
      app: geocore-api
    ports:
      - port: 8080
        targetPort: 8080
        name: http
  